name: static-anal
on: [push]
jobs:
  eslit-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1
      - uses: dunklesToast/eslint-action
      - run: echo eslit validation
      - run: echo thats easy
